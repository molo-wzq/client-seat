# 06: 后台摄入优秀录音

**What to build:** 将优秀电话录音的慢速转写放到后台处理,生成带说话人的待校对转写稿,再进入现有素材提炼与发布流程。前端不提供录音上传、处理等待或原声回放。

**Blocked by:** 02: 完成策略提炼与人工发布; 05: 完成策略解释与文本素材追溯.

**Status:** ready-for-human

L4 范围改为后台录音摄入。当前采用显式 CLI 任务,单次处理一段本地录音并输出 Git 忽略的待校对稿;校对与脱敏后再人工交接到现有素材库。原声回放、音频片段定位、实时语音对练均不在范围内。

- [x] 后台任务可接收一段优秀录音,慢速转写不占用前端对局流程。
- [x] 产出带说话人的待校对转写稿,可交接到现有素材库流程。
- [x] 失败可重试且不会产生半成品素材。
- [x] 后台转写服务具有稳定的契约测试。
- [x] 使用一段真实录音完成后台转写到素材交接的验收。

## Comments

- 2026-09-09 用户重新收窄 L4:只要“更方便地摄入优秀录音”;回放录音没有必要,设计保持精简。删除原声回放、时间片段定位要求,也不提前放置禁用入口。
- 2026-09-09 前端验收后再次调整:转写体感太慢,L4 后续转为后台处理;撤回前端上传实现,同时从第 1 幕移除“自制素材”。后台方案待重新拆解。
- 2026-09-09 后台实现完成:新增 `npm run audio:ingest -- <录音文件> [--out <待校对稿.md>]`;支持 mp3/m4a/wav/webm/ogg、25MB 上限,先调用 `mimo-v2.5-asr` 生成原始文字,再由文本模型整理说话人与轮次。默认产物落在 Git 忽略的 `data/audio-intake/`,写入采用临时文件后原子改名;任一阶段失败均不留半成品,且不会修改产品素材库。前端无新增入口。
- 2026-09-09 首次真实录音验收发现当前 ASR 网关拒绝同一请求中的文字 part(`ASR request must not include text parts; text prompt is injected by the gateway`);已将适配器改为纯音频请求,契约测试同步钉住该限制。失败未产生半成品。
- 2026-09-09 使用 `l1/audio/audio-02-c.mp3` 完成真实链路验收:生成 18 个连续编号轮次(经理 10、客户 8),关键业务事实抽查全部保留,产物带“待校对、待脱敏”警示并停留在 Git 忽略目录,可人工校对后交接到既有素材流程。纯 ASR 不提供说话人标签,因此确定采用“纯音频转写 + 独立文本整理”的两阶段后台链路。
