## Agent skills

### Issue tracker

任务和规格使用本地 Markdown，保存在 `.scratch/`。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认状态：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。详见 `docs/agents/triage-labels.md`。

### Domain docs

项目采用单一上下文，领域词汇记录在根目录 `CONTEXT.md`，重要决策记录在 `docs/adr/`。详见 `docs/agents/domain.md`。
